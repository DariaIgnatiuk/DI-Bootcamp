// Exercise 1 : Menu
// Using the array below:

const menu = [
  {
    type : "starter",
    name : "Houmous with Pita"
  },
  {
    type : "starter",
    name : "Vegetable Soup with Houmous peas"
  },
  {
    type : "dessert",
    name : "Chocolate Cake"
  }
]

// 1. Using an array method and ternary operator, check if at least one element in the menu array is a dessert.

// console.log(menu.some((item) => {return item.type === 'dessert'; 
// }))

// 2. Using an array method, check if all the elements in the array are starters.

// let result = menu.every((item) => {return item.type === 'starter';})
// console.log(result);

// 3. Using an array method, check if there is at least one element in the array that is a main course. If not, 
// add a main course of your choice to the array.

